import { ScreenStub } from '@/components/dev/ScreenStub'

export const metadata = { title: 'Cancellation & Refunds' }

export default function Page() {
  return <ScreenStub title={'Cancellation & Refunds'} route={'/legal/cancellation'} />
}
