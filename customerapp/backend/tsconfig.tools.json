{
  "//": "Covers the scripts that run beside the functions rather than inside them — the seed, the technician simulator and the rules tests. They live at backend/ level per the repo layout, so they need a tsconfig of their own; functions/tsconfig.json compiles only what is deployed.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "CommonJS",
    "moduleResolution": "Node",
    "noEmit": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": ["seed/**/*.ts", "scripts/**/*.ts", "test/**/*.ts"]
}
